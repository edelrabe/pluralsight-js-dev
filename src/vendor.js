/**
 * This file contains references to the vendor libs.
 * This is used by webpack in prod build only.
 * It's useful since this code won't change as often as the application's code.
 *
 */

/* eslint-disable no-unused-vars */
import fetch from 'whatwg-fetch';
